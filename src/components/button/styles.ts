import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: "#1b04e8",
        height: 48,
        width: "100%",
        alignItems: "center",
        justifyContent: "center",
        borderRadius: 8,

    },
    title: {

        color: "#FFFFFF",
        fontSize: 14,
        fontWeight: 600,
    }

})